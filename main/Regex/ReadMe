.  匹配除换行符以外的任意字符
\w 匹配字母或数字或下划线(不匹配汉字) 等价于 [A-Za-z0-9_]
\W 匹配 非 字母或数字或下划线或汉字 等价于 [^A-Za-z0-9_]
\s 匹配任意的空白符
\S 匹配 非 空白符
\d 匹配数字
\b 匹配单词的开始或结束
^  匹配字符串的开始
$  匹配字符串的结束
^[a-z]+$ 匹配纯字母
^[0-9]+$ 匹配纯数字
[\u4e00-\u9fa5] 匹配汉字
c(a|u|har|onduc|our)t 匹配cat、cut、chart，conduct，court

匹配特定范围的数字
65530-65535  ==>  6553[0-5]          末位区间0-5
65500-65529  ==>  655[0-2][0-9]      第四位区间0-2，末位区间0-9
65000-65499  ==>  65[0-4][0-9]{2}    第三位区间0-4，后两位0-9
60000-64999  ==>  6[0-4][0-9]{3}     第二位区间0-4，后三位0-9
10000-59999  ==>  [1-5][0-9]{4}      第一位区间1-5，后四位0-9
1-9999       ==>  [1-9][0-9]{0,3}    第一位只能是1-9，后三位可有可无

环视的作用相当于对其所在位置加了一个附加条件，只有满足这个条件，环视子表达式才能匹配成功。
环视主要有以下4个用法：
(?<=exp) 匹配前面是exp的数据
(?<!exp) 匹配前面不是exp的数据
(?=exp) 匹配后面是exp的数据
(?!exp) 匹配后面不是exp的数据

示例1：
(?<=B)AAA 匹配前面是B的AAA，即BAAA匹配，而CAAA不匹配
(?<!B)AAA 匹配前面不是B的AAA，即CAAA匹配，而BAAA不匹配
AAA(?=B) 匹配后面是B的AAA，即AAAB匹配，而AAAC不匹配
AAA(?!B) 匹配后面不是B的AAA，即AAAC能匹配，而AAAB不能匹配

示例2：
特殊用法，将原本应该放在前面的 环视 放在后面
[A-Z](?<=B)   [A-Z]范围等于B，只匹配B
[A-Z](?<!B)   [A-Z]范围排除B，除了B都匹配
(?=B)[A-Z]    [A-Z]范围等于B，只匹配B
(?!B)[A-Z]    [A-Z]范围排除B，除了B都匹配
